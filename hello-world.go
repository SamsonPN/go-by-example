package main

import "fmt"

func main() {
	fmt.Println("hello world")
}

/**
* can run a go program by doing this:

go run [file-name].go

* go build [file-name].go to create a binary
* then you can just run it by doing ./[file-name]
*/
